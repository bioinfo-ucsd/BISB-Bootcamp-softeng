def alexmonell(value):
    """Counts the number of occurrences of `substring` in `string`

    Parameters
    ----------
    string : value
        The string to improve

    Returns
    -------
    string
        The new string

    """
    astring = "Your string was: "+str(value)+" but a better one is - "
    return astring + "alex was here"
